use std::biguint;

fn main(pub lhs: Field, rhs: Field) -> biguint::Result {
    let mod_bits = 3;
    let another_val = 3;
    let res = biguint::module_sum_three(mod_bits, lhs, rhs, another_val);
    return res;
}