struct Thing {
    x: Field,
    y: Field,
}

fn Thing.check(self) -> Field {
    assert_eq(self.x, 1);
    assert_eq(self.y, 2);

    return self.x + self.y;
}

fn main(pub x: Field) {
    let thing = Thing { x: x, y: x + 1 };
    let res = thing.check();
    assert_eq(res, 3);
}
