hint fn ite(xx: Field , arr: [Field;LEN]) -> Field {
    let mut var = 0;
    if xx == 10 {
        var = xx;
        for idx in 0..LEN {
            var = var + arr[idx];
        }
        return xx * var;
    } else {
        return xx * xx;
    }
}


fn main(pub xx: Field) {
    let mut plus = xx + 1;
    let cond = xx == 1;
    let ans = unsafe ite(plus, [1,2,3]);
    log(ans);
    assert_eq(ans, 4);
    assert_eq(plus, 2);
}
